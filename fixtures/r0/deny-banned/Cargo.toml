[package]
name = "relay-r0-deny-banned"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
publish = false

[dependencies]
ulid = { version = "=0.0.0", path = "ulid-probe" }

[workspace]
